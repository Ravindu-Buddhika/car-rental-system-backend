spring:
  datasource:
    url: jdbc:mysql://localhost:3306/carmanagement?createDatabaseIfNotExist = true
    username:
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username:
    password:
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true